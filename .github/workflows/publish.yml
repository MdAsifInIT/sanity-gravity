name: Reusable Publish

on:
  workflow_call:
    inputs:
      repo_lc:
        required: true
        type: string
      tag:
        required: true
        type: string

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Promote and Publish (All Architectures)
        run: |
          REPO_LC="${{ inputs.repo_lc }}"
          TAG_VAL="${{ inputs.tag }}"
          
          # Pull x64 and arm64 transient images
          docker pull ghcr.io/${REPO_LC}-core:${TAG_VAL}-amd64
          docker pull ghcr.io/${REPO_LC}-core:${TAG_VAL}-arm64
          
          # Create and push multi-arch manifest
          docker manifest create ghcr.io/${REPO_LC}-core:latest \
            ghcr.io/${REPO_LC}-core:${TAG_VAL}-amd64 \
            ghcr.io/${REPO_LC}-core:${TAG_VAL}-arm64
          
          docker manifest push ghcr.io/${REPO_LC}-core:latest

          # [Optional] Push specific release tag if applicable
          if [ "${{ github.event_name }}" == "release" ]; then
            TAG_NAME=${{ github.event.release.tag_name }}
            docker manifest create ghcr.io/${REPO_LC}-core:${TAG_NAME} \
              ghcr.io/${REPO_LC}-core:${TAG_VAL}-amd64 \
              ghcr.io/${REPO_LC}-core:${TAG_VAL}-arm64
            docker manifest push ghcr.io/${REPO_LC}-core:${TAG_NAME}
          fi
